<Window x:Class="Family.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Family"
        xmlns:uBuilt="clr-namespace:Family.customcontrols"
        mc:Ignorable="d"
        Background="Transparent" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="MainWindow" Height="800" Width="1438" WindowStyle="None">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="resources/icons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Border CornerRadius="30" Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <StackPanel Orientation="Vertical" Margin="0,5,0,0">
                    <Image Source="/assets/logo.png" Width="150" Height="150"/>

                    <uBuilt:MenuButton IsSelected="True" Text="My Dashboard" Padding="20,0,0,0" Icon="{StaticResource Dashboard}" GroupName="MenuButton"/>

                    <uBuilt:MenuButton Text="Spending Report" Padding="20,0,0,0" Icon="{StaticResource Money}" GroupName="MenuButton"/>

                    <uBuilt:MenuButton Text="Chat" Padding="20,0,0,0" Icon="{StaticResource Chat}" GroupName="MenuButton"/>

                    <Rectangle Fill="LightGray" Height="2" Margin="5"/>

                    <uBuilt:MenuButton Text="Accounts" Padding="20,0,0,0" Icon="{StaticResource Accounts}" GroupName="MenuButton"/>

                    <uBuilt:MenuButton Text="Settings" Padding="20,0,0,0" Icon="{StaticResource Settings}" GroupName="MenuButton"/>
                </StackPanel>
            </Grid>
            <Grid Grid.Column="1">
                
            </Grid>
        </Grid>
    </Border>
</Window>
